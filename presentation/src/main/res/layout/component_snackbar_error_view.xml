<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <androidx.cardview.widget.CardView
        style="@style/Card"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin_medium"
        android:layout_marginEnd="@dimen/margin_medium"
        app:cardBackgroundColor="@color/feedback_error_background"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/content_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/feedback_icon"
                style="@style/Icon.SemiSmall"
                android:layout_marginStart="@dimen/margin_standard_large"
                android:contentDescription="@string/error_feedback_image_content_description"
                android:src="@drawable/ic_error"
                app:tint="@color/error"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:tint="@color/error"/>

            <TextView
                android:id="@+id/message"
                style="@style/Text.Subtitle2"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginVertical="@dimen/margin_standard_large"
                android:layout_marginStart="@dimen/margin_big"
                android:layout_marginEnd="@dimen/margin_big"
                android:ellipsize="end"
                android:maxLines="3"
                android:textColor="@color/secondaryTextColor"
                android:gravity="center"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toStartOf="@id/fun_icon"
                app:layout_constraintStart_toEndOf="@id/feedback_icon"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="@string/error_default" />

            <ImageView
                android:id="@+id/fun_icon"
                style="@style/Icon"
                android:layout_marginEnd="@dimen/margin_standard_large"
                android:contentDescription="@string/error_feedback_fun_image_content_description"
                android:src="@drawable/ic_darth_vader"
                app:tint="@color/secondaryTextColor"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:tint="@color/secondaryTextColor"/>

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.cardview.widget.CardView>

</androidx.constraintlayout.widget.ConstraintLayout>

